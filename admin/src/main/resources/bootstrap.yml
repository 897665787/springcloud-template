# bootstrap是系统级配置文件，优先级高于application.yml，建议不可变的配置放在bootstrap.yml中
server:
  port: 5001
  tomcat:
    min-spare-threads: 5
    max-threads: 100
    #servlet:
    #context-path: /admin

spring:
  application:
    name: template-admin
  profiles:
    active: dev # dev,test,pre,prod，不同环境jar启动命令后面加上 --spring.profiles.active=prod
    #include: eureka,nacos,actuator # 引入配置文件：bootstrap-*.yml