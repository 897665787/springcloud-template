package ${package.Controller};

import com.company.common.api.Result;
import org.springframework.web.bind.annotation.*;
import org.springframework.beans.factory.annotation.Autowired;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;

/**
 * ${table.comment} 前端控制器
 * @author CodeGenerate
 * @since ${date}
 */
@Api(tags = "${table.comment}管理")
@RestController
@RequestMapping("/${table.name}")
public class ${table.controllerName} {
    
    @Autowired
    private ${package.Service}.${table.serviceName} ${table.serviceName?uncap_first};
    
    /**
     * 分页查询${table.comment}
     */
    @ApiOperation("分页查询${table.comment}")
    @GetMapping("/page")
    public Result<Page<${package.Entity}.${entity}>> page(
        @RequestParam(defaultValue = "1") Long current,
        @RequestParam(defaultValue = "10") Long size) {
        Page<${package.Entity}.${entity}> page = new Page<>(current, size);
        return Result.success(${table.serviceName?uncap_first}.page(page));
    }
    
    /**
     * 根据ID查询${table.comment}
     */
    @ApiOperation("根据ID查询${table.comment}")
    @GetMapping("/query")
    public Result<${package.Entity}.${entity}> query(@RequestParam Integer id) {
        return Result.success(${table.serviceName?uncap_first}.getById(id));
    }
    
    /**
     * 保存${table.comment}
     */
    @ApiOperation("保存${table.comment}")
    @PostMapping("/save")
    public Result<Boolean> save(@RequestBody ${package.Entity}.${entity} ${entity?uncap_first}) {
        return Result.success(${table.serviceName?uncap_first}.save(${entity?uncap_first}));
    }
    
    /**
     * 更新${table.comment}
     */
    @ApiOperation("更新${table.comment}")
    @PostMapping("/update")
    public Result<Boolean> update(@RequestBody ${package.Entity}.${entity} ${entity?uncap_first}) {
        return Result.success(${table.serviceName?uncap_first}.updateById(${entity?uncap_first}));
    }
    
    /**
     * 删除${table.comment}
     */
    @ApiOperation("删除${table.comment}")
    @PostMapping("/remove")
    public Result<Boolean> remove(@RequestBody Integer id) {
        return Result.success(${table.serviceName?uncap_first}.removeById(id));
    }
}