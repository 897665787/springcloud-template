spring:
  application:
    name: template-order
    
template:
  domain: http://t-domain.com
  enable:
    redis: false
    redisson: false
    idempotent: false
    rabbitmq: false
    data-encypt: false
    access-control: false
