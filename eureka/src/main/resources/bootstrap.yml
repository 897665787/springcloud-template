# bootstrap是系统级配置文件，优先级高于application.yml，建议不可变的配置放在bootstrap.yml中
server:
  port: 7001
  tomcat:
    min-spare-threads: 5
    max-threads: 100

spring:
  application:
    name: template-eureka
  profiles:
    active: dev
