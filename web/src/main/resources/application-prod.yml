spring:
  profiles:
    include: eureka-prod,redis-prod,rabbitmq-prod,apollo-prod
    
template:
  domain: http://domain.com
  enable:
    redis: true
    redisson: true
    idempotent: false
    rabbitmq: true
    data-encypt: false
  mock:
    time: false
