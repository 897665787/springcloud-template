spring:
  profiles:
    include: routes,eureka-prod,rabbitmq-prod,apollo-prod

template:
  domain: http://domain.com
  enable:
    rabbitmq: true
    access-control: true

# Token 配置
token:
  # 密钥
  secret: hxqhjvtam5
