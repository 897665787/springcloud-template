template:
  domain: http://domain.com
  enable:
    redis: true
    rabbitmq: true
    
spring:
  profiles:
    include: routes,rabbitmq-prod,apollo-prod

# Token 配置
token:
  # 密钥
  secret: hxqhjvtam5
